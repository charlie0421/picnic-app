import 'package:flutter/material.dart';

/// Stable, high-contrast campaign colors shared by every candy boost surface.
///
/// App theme colors are intentionally not used here: production can configure
/// them to light pastels that do not keep small white promotional copy legible.
const kCandyBoostPurple = Color(0xFF6C00F5);
const kCandyBoostPink = Color(0xFFFF0266);
