---
description: 
globs: 
alwaysApply: true
---
---
name: 컴포넌트 네이밍
description: 컴포넌트 및 훅 네이밍 규칙
pattern: **/*.{ts,tsx}
---

```json
{
  "rules": [
    {
      "name": "컴포넌트 네이밍",
      "description": "React 컴포넌트 이름 규칙",
      "pattern": "**/*.tsx",
      "rules": [
        "PascalCase 사용 (예: Button, UserProfile)",
        "컴포넌트 파일명과 컴포넌트 이름이 일치해야 함",
        "기능을 명확하게 표현하는 이름 사용",
        "접미사로 컴포넌트 타입을 명시 (예: Form, Card, List)"
      ]
    },
    {
      "name": "훅 네이밍",
      "description": "React 훅 이름 규칙",
      "pattern": "**/hooks/*.ts",
      "rules": [
        "use 접두사 사용 (예: useFetch, useAuth)",
        "기능을 명확하게 표현하는 이름 사용",
        "동사로 시작 (예: useGetData, useUpdateUser)"
      ]
    },
    {
      "name": "유틸리티 함수 네이밍",
      "description": "유틸리티 함수 이름 규칙",
      "pattern": "**/utils/*.ts",
      "rules": [
        "camelCase 사용 (예: formatDate, validateEmail)",
        "동사로 시작",
        "기능을 명확하게 표현"
      ]
    }
  ]
}
```